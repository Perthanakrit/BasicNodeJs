function Rabbit(name, bread) { // constructor function
  this.name = name;
  this.bread = bread;
}

//module.exports = Rabbit; // export the constructor

export default Rabbit; // export the constructor
